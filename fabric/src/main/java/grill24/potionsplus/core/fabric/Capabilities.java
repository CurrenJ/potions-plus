package grill24.potionsplus.core.fabric;

/**
 * PHASE 7 (event surface): the Clothesline item-storage capability (fabric-transfer-api-v1
 * ItemStorage.SIDED) is wired here once the ClotheslineBlock/BlockEntity are ported to common. The
 * BE-block classes are neoforge-only until a later phase, so there is nothing to register yet.
 */
public final class Capabilities {
    private Capabilities() {
    }

    public static void register() {
    }
}
