package grill24.potionsplus.core.fabric;

/**
 * No custom command argument types are registered yet (the empty NeoForge DeferredRegister has no
 * entries). Kept for structural parity with the NeoForge/Forge registration hubs.
 */
public class CommandArgumentTypes {
    public static void init() {
    }
}
