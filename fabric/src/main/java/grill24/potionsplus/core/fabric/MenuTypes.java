package grill24.potionsplus.core.fabric;

/**
 * No custom menu types are registered yet (the empty NeoForge DeferredRegister has no entries).
 * Kept for structural parity with the NeoForge/Forge registration hubs.
 */
public class MenuTypes {
    public static void init() {
    }
}
