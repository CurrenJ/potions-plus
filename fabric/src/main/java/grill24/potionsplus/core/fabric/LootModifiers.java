package grill24.potionsplus.core.fabric;

/**
 * PHASE 7 (event surface): the Wormroot + AddMobEffects global loot modifiers hook
 * {@code LootTableEvents.MODIFY_DROPS} here on Fabric (no unified GLM system). Deferred until the
 * alchemy/loot dependency classes are ported to common.
 */
public final class LootModifiers {
    private LootModifiers() {
    }

    public static void register() {
    }
}
