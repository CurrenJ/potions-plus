package grill24.potionsplus.core.forge;

/**
 * PHASE 7 (event surface): the Clothesline item-storage capability (ForgeCapabilities.ITEM_HANDLER)
 * is wired here once the ClotheslineBlock/BlockEntity are ported to common. The BE-block classes are
 * neoforge-only until a later phase, so there is nothing to register yet.
 */
public final class Capabilities {
    private Capabilities() {
    }

    public static void register() {
    }
}
