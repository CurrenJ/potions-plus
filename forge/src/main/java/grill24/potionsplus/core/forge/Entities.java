package grill24.potionsplus.core.forge;

/**
 * No custom entity types are registered (the 1.21.1 NeoForge hub is an empty DeferredRegister, and
 * there is no common {@code core.Entities} hub). Kept for structural parity.
 */
public class Entities {
    public static void init() {
    }
}
