# Forge equivalent of common/src/main/resources/potionsplus.accesswidener — same members, AT
# syntax. NeoForge and Fabric both get the widened access baked into their loom-transformed
# Minecraft jar from the shared accessWidener; Forge's production jar is patched separately via
# this file (the AccessTransformers library), so every entry here must mirror the accesswidener
# (Decision 4 / Phase 9 — docs/multi-loader-expansion.md). Nothing in forge/ needs these yet
# (Phase 11 client work is what will), but they're authored now so the two files can't drift.

# Block.popExperience — protected in vanilla
public net.minecraft.world.level.block.Block popExperience(Lnet/minecraft/server/level/ServerLevel;Lnet/minecraft/core/BlockPos;I)V

# HolderSetCodec.homogenousListCodec — private field
public net.minecraft.resources.HolderSetCodec homogenousListCodec

# TrackingEmitter — private fields accessed by particle code
public net.minecraft.client.particle.TrackingEmitter entity
public-f net.minecraft.client.particle.TrackingEmitter life
public net.minecraft.client.particle.TrackingEmitter lifeTime
public net.minecraft.client.particle.TrackingEmitter particleType

# RecipeManager.byType / byName — private fields; core.neoforge.RecipesRegistrar's runtime recipe
# injection mutates these directly. Mirrors neoforge/src/main/resources/META-INF/accesstransformer.cfg's
# pre-existing entries (added before Phase 9, working there) and common's accesswidener (added Phase 9).
public net.minecraft.world.item.crafting.RecipeManager byType
public net.minecraft.world.item.crafting.RecipeManager byName
